<h4></h4>

<div class="card" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{produto.nome}}</h5>
    <h6 class="card-subtitle mb-2 text-muted">R$ {{produto.preco}},00</h6>
    <p class="card-text">Estoque: {{produto.estoque}}</p>
    <a href="/produto/update/{{produto.id}}" class="card-link">Editar</a>
    <a href="/produto/remove/{{produto.id}}" class="card-link">Apagar</a>
  </div>
</div>

<div class="mt-4 fs-4" ><a href="/produto"><i class="bi bi-arrow-left-circle-fill"></i></a></div>